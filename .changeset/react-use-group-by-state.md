---
"@gridkitjs/react": minor
---

New `useGroupByState(gridRef)` hook: reactive group-by and group-expansion state read off a mounted `DataGridComponent`'s `ref`, with `expandAllGroups`/`collapseAllGroups` actions, for a custom group-by UI living outside the grid's own DOM.
