---
"@gridkitjs/theme-tailwind": minor
---

Styles for aggregates (`@gridkitjs/react`'s new `aggregates` prop): `.group-aggregates`/`.group-aggregate` for a group header's inline subtotal, and `.grid-footer`/`.grid-footer-cell` for the grand-total footer, including its `borders-*` variants.
